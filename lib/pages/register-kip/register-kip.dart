import 'package:flutter/material.dart';

class RegisterKip extends StatelessWidget {
  const RegisterKip({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Container(),
    );
  }
}